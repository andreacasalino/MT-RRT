size_t -> std::size_t
rendere const attributi il piu possibile
rendere const method il piu possibile
collassare Node e Trajectory ad essere la stessa classe?
usare explicit in c'tor
che fare con DISPLAY_ITERATIONS?
replace rand with thread safe rand everywhere
quando calcolo rewird devo eseguirlo per il pivot o no?
flag per usare seed deterministico o random
mettere documentazione latex
ricontrollare bene funzione che fa i rewirds
ricontrollare tutti ++
randomizzare seme all'interno di thread
improve code recycle between solvers
migliorare gather master star
