# just a function to check that common kit can be accessed
def okIsWorking():
    print('===========================>>> called from {}, this proves that the common package can be imported'.format(__file__))
